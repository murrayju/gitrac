name: Publish

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.os }}-${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - os: linux
            arch: x64
            runner: ubuntu-latest
            target: bun-linux-x64
          - os: linux
            arch: arm64
            runner: ubuntu-24.04-arm
            target: bun-linux-arm64
          - os: darwin
            arch: arm64
            runner: macos-latest
            target: bun-darwin-arm64

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: ./bun install

      - name: Build binary
        run: |
          ./bun run scripts/build.ts \
            --minify --sourcemap \
            --target=${{ matrix.target }} \
            --outfile=gitrac-${{ matrix.os }}-${{ matrix.arch }}

      - name: Bundle UI assets with binary
        run: tar czf gitrac-${{ matrix.os }}-${{ matrix.arch }}.tar.gz gitrac-${{ matrix.os }}-${{ matrix.arch }} dist/ui/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gitrac-${{ matrix.os }}-${{ matrix.arch }}
          path: gitrac-${{ matrix.os }}-${{ matrix.arch }}.tar.gz

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./binaries
          merge-multiple: true

      - name: List binaries
        run: ls -la ./binaries

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: ./binaries/*
          generate_release_notes: true
